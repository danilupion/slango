---
'@slango.configs/oxlint': minor
'@slango.configs/lint-staged': minor
---

Add `@slango.configs/oxlint` presets (oxlint's `correctness` category + tsgolint type-aware rules, perfectionist tiers and regexp recommended via JS plugins; replaces `@slango.configs/eslint` on TypeScript 7) and `javascript-oxlint` / `typescript-oxlint` / `typescript-oxlint-no-tests` lint-staged presets.
